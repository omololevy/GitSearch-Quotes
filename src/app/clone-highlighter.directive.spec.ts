import { CloneHighlighterDirective } from './clone-highlighter.directive';

describe('CloneHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new CloneHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
